# ShadowDict — 시트콤 받아쓰기 영어 학습 앱 · 프로젝트 사양서

> **이 문서는 Claude Code에 그대로 붙여넣어 개발을 시작하기 위한 사양서입니다.**
> 구현 시 이 문서의 명세를 기준으로 삼고, 명세에 없는 부분은 질문하거나 가장 단순한 방식을 선택하세요.

---

## 0. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 앱 이름 | ShadowDict (가칭) |
| 목적 | 영어 시트콤/영상 대사를 하루 10문장씩 4단계로 학습해 완전히 체득 |
| 플랫폼 | Android (최소 SDK 26 / 타깃 SDK 35), 갤럭시 폰 우선 최적화 |
| 서버 | **없음.** 전부 로컬(온디바이스). 네트워크는 유튜브 재생 시에만 사용 |
| 사용자 | 단일 사용자(본인). 로그인·계정 없음 |
| 핵심 차별점 | 3단계 **받아쓰기 채점**(단어 단위 색상 피드백) + 스트릭 강제 습관화 |

### 절대 원칙
1. **서버를 만들지 않는다.** 모든 데이터는 Room DB + 앱 내부 저장소에 보관.
2. **콘텐츠를 앱에 내장하지 않는다.** 아래 §2 콘텐츠 정책 참조.
3. **오프라인에서 로컬 영상 학습이 완전히 동작해야 한다.**

---

## 1. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | Kotlin | — |
| UI | Jetpack Compose (Material 3) | — |
| 아키텍처 | MVVM + Repository, 단일 Activity + Navigation Compose | — |
| DB | Room | 마이그레이션 대비해 스키마 export 켤 것 |
| 설정 저장 | DataStore (Preferences) | 알림 시각, 일일 목표 수 등 |
| 로컬 영상/오디오 | AndroidX Media3 (ExoPlayer) | 구간 반복의 정밀도가 핵심 |
| 유튜브 재생 | `com.pierfrancescosoffritti.androidyoutubeplayer` | IFrame Player API의 WebView 래퍼 |
| 백그라운드 작업 | WorkManager | 스트릭 체크·알림 |
| 알림 | NotificationCompat | Android 13+ `POST_NOTIFICATIONS` 런타임 권한 필수 |
| 위젯 | Glance (androidx.glance) | 스트릭 표시 |
| TTS | Android 내장 TextToSpeech | 4단계 복습 시 보조 |
| 비동기 | Coroutines + Flow | — |

### 기술적 주의사항 (반드시 반영)
- **구글의 YouTube Android Player API는 deprecated 상태**이며, 구글은 WebView 안에서 IFrame Player API를 쓰도록 권고합니다. 위 라이브러리가 이를 감싼 것이므로 이것을 사용하세요.
- **유튜브 플레이어 위에 오버레이를 덮지 마세요.** YouTube API 정책상 플레이어의 표준 재생 기능을 가리거나 수정하는 것은 금지됩니다. 자막은 반드시 **플레이어 영역 바깥, 아래쪽 별도 카드**에 렌더링합니다.
- **삼성 갤럭시의 공격적 배터리 최적화**로 WorkManager 알림이 누락될 수 있습니다. 온보딩에서 배터리 최적화 예외 요청 화면을 반드시 포함하세요.
- `SCHEDULE_EXACT_ALARM`은 Android 12+에서 제한이 있으므로 **정확 알람 대신 WorkManager의 유연 스케줄**을 사용합니다(±15분 오차 허용).

---

## 2. 콘텐츠 정책 (설계 제약)

**앱에 영상이나 자막을 번들·다운로드·스크래핑하지 않습니다.** 앱은 "빈 껍데기"이고 학습 소재는 사용자가 공급합니다.

콘텐츠 소스 3종을 지원:

| 소스 타입 | 영상 | 자막 | 받아쓰기(3단계) |
|---|---|---|---|
| `LOCAL_FILE` | 기기 내 영상/오디오 파일 (SAF로 선택) | 사용자가 .srt/.vtt 선택 | 허용 |
| `YOUTUBE` | 유튜브 영상 ID (임베드 재생) | 사용자가 .srt/.vtt 별도 제공 | 자막이 수동 제작본일 때만 허용 |
| `TEXT_ONLY` | 없음 (TTS로 대체) | 사용자가 텍스트 붙여넣기 | 허용 |

**중요:** 유튜브 자동 생성 자막은 문장부호가 없고 단어 오류가 있어 받아쓰기 정답으로 쓸 수 없습니다. 자막 등록 시 `isAutoGenerated` 플래그를 사용자가 지정하게 하고, `true`이면 3단계(받아쓰기)를 비활성화하고 1·2·4단계만 진행합니다.

---

## 3. 데이터 모델 (Room)

```kotlin
// ─────────────────────────────────────────────
// 학습 소재 (에피소드 1개 = Source 1개)
// ─────────────────────────────────────────────
@Entity(tableName = "sources")
data class Source(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val title: String,                 // "Friends S01E01" 등 사용자 입력
    val type: SourceType,              // LOCAL_FILE / YOUTUBE / TEXT_ONLY
    val mediaUri: String?,             // SAF persistable URI 또는 youtube videoId
    val subtitleFileName: String?,     // 원본 자막 파일명 (표시용)
    val isAutoGenerated: Boolean,      // true면 받아쓰기 단계 비활성화
    val totalLines: Int,
    val createdAt: Long,
    val lastStudiedAt: Long?
)

enum class SourceType { LOCAL_FILE, YOUTUBE, TEXT_ONLY }

// ─────────────────────────────────────────────
// 대사 1줄 = 학습 카드 1장
// ─────────────────────────────────────────────
@Entity(
    tableName = "lines",
    foreignKeys = [ForeignKey(
        entity = Source::class, parentColumns = ["id"],
        childColumns = ["sourceId"], onDelete = ForeignKey.CASCADE
    )],
    indices = [Index("sourceId"), Index("nextReviewAt")]
)
data class SubLine(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val sourceId: Long,
    val orderIndex: Int,               // 자막 내 순번 (0부터)
    val startMs: Long,                 // 구간 반복 시작
    val endMs: Long,                   // 구간 반복 끝
    val textEn: String,                // 정답 원문
    val textKo: String?,               // 한글 뜻 (사용자가 2단계에서 입력하거나 사전 등록)
    val chunkParentId: Long?,          // 긴 문장을 쪼갠 경우 원본 라인 id
    val wordCount: Int,

    // ── 학습 상태 ──
    val status: LineStatus,            // NEW / LEARNING / MASTERED
    val firstTryAccuracy: Float?,      // 첫 시도 정확도 (실력 지표. 절대 덮어쓰지 말 것)
    val bestAccuracy: Float,
    val attemptCount: Int,             // 받아쓰기 총 시도 횟수
    val replayCount: Int,              // 오디오 재생 횟수
    val hintLevelUsed: Int,            // 사용한 최고 힌트 단계 (0~4)

    // ── 간격 반복 ──
    val srsStage: Int,                 // 0,1,2,3,4 → 0/1/3/7/14일
    val nextReviewAt: Long?,           // epoch millis. null이면 미학습
    val completedAt: Long?
)

enum class LineStatus { NEW, LEARNING, MASTERED }

// ─────────────────────────────────────────────
// 일일 세션 (스트릭 계산의 근거)
// ─────────────────────────────────────────────
@Entity(tableName = "daily_sessions")
data class DailySession(
    @PrimaryKey val date: String,      // "2026-08-01" (LocalDate.toString(), 기기 로컬 타임존)
    val newLinesDone: Int,
    val reviewLinesDone: Int,
    val goalCount: Int,                // 그날의 목표 (기본 10)
    val isCompleted: Boolean,
    val usedFreeze: Boolean,           // 스트릭 프리즈 사용 여부
    val studySeconds: Int,
    val completedAt: Long?
)

// ─────────────────────────────────────────────
// 시도 기록 (분석·복습용, 선택적이지만 권장)
// ─────────────────────────────────────────────
@Entity(tableName = "attempts")
data class Attempt(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val lineId: Long,
    val userInput: String,
    val accuracy: Float,
    val hintLevel: Int,
    val createdAt: Long
)
```

### DataStore 설정값

```
reminderHour: Int = 21          // 알림 시각(시)
reminderMinute: Int = 0
dailyGoal: Int = 10             // 하루 신규 문장 수
reviewGoal: Int = 5             // 하루 복습 문장 수
freezeRemaining: Int = 1        // 남은 스트릭 프리즈 (주 1회 충전)
freezeRechargedWeek: String     // "2026-W31"
strictPunctuation: Boolean = false
chunkThreshold: Int = 12        // 이 단어 수 초과 시 자동 분할
playbackSpeed: Float = 1.0f
```

---

## 4. 학습 플로우

### 세션 구성
```
하루 1세션 = [복습 5문장] → [신규 10문장]
복습이 없으면 신규만 진행. 신규가 소진되면 복습만 진행.
```

### 문장 1개당 4단계 (순서 고정, 건너뛰기 불가)

| 단계 | 이름 | 화면에 보이는 것 | 사용자 행동 | 통과 조건 |
|---|---|---|---|---|
| 1 | LISTEN | 자막 완전 숨김 | 구간 반복 청취 | 3회 재생 완료 |
| 2 | MEANING | 영어 자막 표시 | 한글 뜻 입력 | 입력 후 "확인" (채점 없음, 자기평가) |
| 3 | **DICTATION** | **아무것도 안 보임** | **소리만 듣고 영어 타이핑** | **정확도 100%** (§6 참조) |
| 4 | IMPRINT | 영어 + 한글 동시 표시 | 소리 들으며 눈으로 확인 | 1회 재생 후 스와이프 |

- `isAutoGenerated == true`인 소스는 3단계를 건너뛰고 1→2→4로 진행합니다.
- 3단계에서 이탈했다가 돌아오면 해당 문장의 3단계부터 재개합니다(진행 상태 저장).

### 청크 분할 규칙
`wordCount > chunkThreshold(기본 12)`인 라인은 등록 시점에 자동 분할합니다.

```
분할 우선순위:
1) 쉼표(,) / 세미콜론(;) / 대시(—) 위치
2) 등위·종속 접속사 앞: and, but, or, so, because, when, if, that, which
3) 위 둘이 없으면 중간 지점에서 강제 분할

각 조각의 startMs/endMs는 원본 구간을 단어 수 비율로 안분(按分).
분할된 조각은 chunkParentId에 원본 id를 저장하고, 4단계(IMPRINT)에서는 
원본 문장 전체를 한 번에 보여준다.
```

---

## 5. 화면 명세

### 5.1 홈 (`HomeScreen`)
```
┌─────────────────────────────┐
│  🔥 27일 연속                │   ← 크게. 오늘 완료 시 불꽃 채색
│  ❄️ 프리즈 1개                │
├─────────────────────────────┤
│  오늘의 학습                  │
│  복습 5 · 신규 10            │
│  ▓▓▓▓▓░░░░░  5/15           │
│                             │
│  [  학습 시작  ]             │   ← 완료 시 "오늘 완료 ✓"
├─────────────────────────────┤
│  내 자료                     │
│  · Friends S01E01  120/240  │
│  · TED - Body Language 30/80│
│  [ + 자료 추가 ]             │
└─────────────────────────────┘
```
- 하단 네비게이션: 홈 / 자료 / 통계 / 설정

### 5.2 자료 추가 (`AddSourceScreen`)
1. 타입 선택 (로컬 / 유튜브 / 텍스트)
2. 미디어 지정 — 로컬은 SAF 파일 선택(**persistable URI permission 반드시 획득**), 유튜브는 URL 붙여넣기 후 videoId 추출
3. 자막 파일 선택 (.srt / .vtt)
4. **"이 자막은 자동 생성 자막입니까?" 체크박스** → `isAutoGenerated`
5. 파싱 미리보기 (앞 5줄 표시) → 확인 후 저장

### 5.3 학습 화면 (`StudyScreen`) — 핵심 화면

공통 상단: `진행도 7/15` · `단계 인디케이터 ①②③④` · 나가기

**1단계 LISTEN**
```
   [ 영상 or 파형 영역 ]
   
   ┌───────────────────┐
   │   ●  ●  ○         │   ← 재생 횟수 3회
   └───────────────────┘
   [ ▶ 다시 듣기 ]  [ 0.75x ]
```

**2단계 MEANING**
```
   "Could I BE any more sarcastic?"     ← 영어 자막 표시
   
   ┌───────────────────┐
   │ 한글 뜻을 써보세요   │
   └───────────────────┘
   [ 확인 ]
```

**3단계 DICTATION** ★ 가장 중요
```
   🔊  (자막 없음)
   
   ┌─────────────────────────────┐
   │ 들은 대로 영어로 입력        │  ← autoCorrect OFF
   └─────────────────────────────┘
   
   ── 채점 결과 (제출 후) ──
   Could I ~~the~~ be ___ more sarcastc
   초록  초록  회색  주황  파랑   초록   주황
   
   정확도 5/7 (71%)   시도 2회
   [ 다시 듣기 ] [ 힌트 ] [ 제출 ]   ← 키보드 바로 위 고정 바
```

**4단계 IMPRINT**
```
   Could I BE any more sarcastic?
   나 더 비꼴 수도 있어?
   
   [ ▶ ]        ← 스와이프하여 다음 문장
```

### 5.4 통계 (`StatsScreen`)
- 최근 30일 스트릭 캘린더(잔디 형태)
- **첫 시도 정확도 추이 꺾은선** ← 진짜 실력 지표
- 총 마스터 문장 수 / 자주 틀리는 단어 Top 20

### 5.5 설정 (`SettingsScreen`)
알림 시각 · 일일 목표 · 청크 임계값 · 문장부호 엄격 모드 · 배터리 최적화 예외 안내 · 데이터 백업(JSON export/import)

### 5.6 위젯 (Glance)
`🔥 27` + 오늘 완료 체크 + 탭 시 학습 화면 바로 진입

---

## 6. 받아쓰기 채점 알고리즘 ★ 핵심

### 6.1 정규화

```
function normalize(word, strictPunctuation):
    w = word
    w = removeAll(w, [".", ",", "!", "?", ";", ":", "\"", "“", "”", "…", "-", "—"])
    w = toLowerCase(w)
    w = replace(w, "’", "'")            // 스마트 아포스트로피 통일
    w = expandContraction(w)            // I'm → i am, don't → do not, it's → it is
    return trim(w)

CONTRACTIONS = {
    "i'm":"i am", "you're":"you are", "he's":"he is", "she's":"she is",
    "it's":"it is", "we're":"we are", "they're":"they are",
    "don't":"do not", "doesn't":"does not", "didn't":"did not",
    "can't":"cannot", "won't":"will not", "isn't":"is not",
    "aren't":"are not", "wasn't":"was not", "weren't":"were not",
    "i've":"i have", "i'll":"i will", "i'd":"i would",
    "that's":"that is", "what's":"what is", "there's":"there is",
    "let's":"let us", "gonna":"going to", "wanna":"want to"
}
// 축약형은 확장 후 여러 토큰이 될 수 있으므로 토크나이즈는 확장 뒤에 수행한다.
```

### 6.2 단어 정렬 (Levenshtein DP)

**통짜 문자열 비교(`==`)는 절대 사용하지 않습니다.** 어디가 틀렸는지 짚어줄 수 없기 때문입니다.

```
function alignWords(answerWords, inputWords):
    n = answerWords.length
    m = inputWords.length
    
    // dp[i][j] = answer 앞 i개와 input 앞 j개를 맞추는 최소 편집 비용
    dp = 2D array (n+1) x (m+1)
    for i in 0..n: dp[i][0] = i
    for j in 0..m: dp[0][j] = j
    
    for i in 1..n:
        for j in 1..m:
            cost = (normalize(answerWords[i-1]) == normalize(inputWords[j-1])) ? 0 : 1
            dp[i][j] = min(
                dp[i-1][j]   + 1,      // 누락 (MISSING)
                dp[i][j-1]   + 1,      // 잉여 (EXTRA)
                dp[i-1][j-1] + cost    // 일치 or 치환
            )
    
    // ── 역추적하여 정렬 결과 생성 ──
    result = empty list
    i = n; j = m
    while i > 0 or j > 0:
        if i > 0 and j > 0 and dp[i][j] == dp[i-1][j-1] + costOf(i,j):
            if costOf(i,j) == 0:
                result.prepend(Token(CORRECT, answerWords[i-1], inputWords[j-1]))
            else:
                // 치환 → 오타인지 다른 단어인지 글자 단위로 재판정
                d = charLevenshtein(normalize(answerWords[i-1]), normalize(inputWords[j-1]))
                maxLen = max(len(a), len(b))
                if d <= 2 and d <= maxLen / 2:
                    result.prepend(Token(TYPO, answerWords[i-1], inputWords[j-1]))
                else:
                    result.prepend(Token(WRONG, answerWords[i-1], inputWords[j-1]))
            i--; j--
        else if i > 0 and dp[i][j] == dp[i-1][j] + 1:
            result.prepend(Token(MISSING, answerWords[i-1], null))
            i--
        else:
            result.prepend(Token(EXTRA, null, inputWords[j-1]))
            j--
    
    return result
```

### 6.3 토큰 렌더링

| TokenType | 색상 | 표시 방식 | 정확도 산입 |
|---|---|---|---|
| `CORRECT` | 초록 | 입력한 단어 그대로 | ○ |
| `TYPO` | 주황 | 입력 단어 + 위첨자로 정답 | △ (0.5점) |
| `WRONG` | 빨강 | 입력 단어(취소선) → 정답 | × |
| `EXTRA` | 회색 | 입력 단어에 취소선 | × (감점) |
| `MISSING` | 파랑 | `___` (글자 수만큼 언더바) | × |

### 6.4 정확도 및 통과 판정

```
function computeAccuracy(tokens):
    total   = count(tokens where type != EXTRA)      // 정답 단어 수
    score   = count(CORRECT) * 1.0 + count(TYPO) * 0.5
    penalty = count(EXTRA) * 0.5
    return clamp((score - penalty) / total, 0.0, 1.0)

function isPassed(tokens, strictPunctuation):
    // 통과 = 의미상 완전 일치
    // 대소문자 / 문장부호 / 축약형 차이는 통과를 막지 않는다 (표시만 함)
    if strictPunctuation:
        return rawText(input) == rawText(answer)
    return count(WRONG) == 0 and count(MISSING) == 0 and count(EXTRA) == 0
    // TYPO는 통과 불가 → 철자까지 맞춰야 함
```

> **설계 의도:** `I'm`을 `Im`으로 썼다고 못 넘어가게 하면 학습이 아니라 고문이 됩니다. 철자 오류(TYPO)는 막되, 표기 관습 차이는 통과시키고 시각적으로만 알려줍니다.

### 6.5 검증용 테스트 케이스 (반드시 유닛 테스트로 작성)

| 정답 | 입력 | 기대 결과 |
|---|---|---|
| `I'm fine.` | `Im fine` | 통과, 100% |
| `I am fine.` | `I'm fine.` | 통과, 100% (축약형 확장) |
| `I could receive it` | `I could recieve it` | 미통과, TYPO 1개, 87.5% |
| `Give me the book` | `Give me book` | 미통과, MISSING 1개, 75% |
| `Give me book` | `Give me the book` | 미통과, EXTRA 1개 |
| `their car` | `they car` | 미통과, WRONG(거리 2지만 짧은 단어) |
| `He said no` | `` (빈 입력) | 미통과, 0%, MISSING 3개 |

---

## 7. 힌트 사다리

한 문장에서 막혀 앱을 꺼버리는 것을 막기 위해 시도 횟수에 따라 힌트를 단계적으로 개방합니다.

| 시도 | 힌트 레벨 | 제공 내용 |
|---|---|---|
| 1회차 | 0 | 없음 |
| 2회차 | 1 | 틀린 단어의 **글자 수**만 표시 `_ _ _ _ _` |
| 3회차 | 2 | 틀린 단어의 **첫 글자** 공개 `c _ _ _ _` |
| 4회차 | 3 | 재생 속도 0.75x 자동 적용 + 첫 두 글자 |
| 5회차 | 4 | **정답 3초 노출** 후 자동 숨김 → 다시 타이핑 |

- 사용한 최고 힌트 레벨을 `SubLine.hintLevelUsed`에 저장.
- 힌트를 썼어도 통과는 통과. 단 `firstTryAccuracy`는 1회차 값으로 고정(덮어쓰기 금지).

---

## 8. 간격 반복 (SRS)

```
통과 시:
    if hintLevelUsed == 0 and attemptCount == 1:
        srsStage += 1                 // 한 번에 맞춤 → 승급
    else if hintLevelUsed >= 3:
        srsStage = max(0, srsStage)   // 힌트 많이 씀 → 제자리
    else:
        srsStage += 1

간격표: stage 0 → 당일 재등장
        stage 1 → +1일
        stage 2 → +3일
        stage 3 → +7일
        stage 4 → +14일 → 이후 status = MASTERED

nextReviewAt = 오늘 자정 + 간격
```

복습 큐 조회:
```sql
SELECT * FROM lines
WHERE nextReviewAt IS NOT NULL
  AND nextReviewAt <= :now
  AND status != 'MASTERED'
ORDER BY nextReviewAt ASC
LIMIT :reviewGoal
```

---

## 9. 스트릭 & 알림

### 스트릭 계산
```
function currentStreak():
    streak = 0
    date = today
    loop:
        session = dailySessionDao.get(date)
        if session != null and (session.isCompleted or session.usedFreeze):
            streak++
            date = date.minusDays(1)
        else if date == today:
            date = date.minusDays(1)      // 오늘은 아직 안 했어도 끊긴 게 아님
        else:
            break
    return streak
```

### 스트릭 프리즈
- 주당 1개 자동 충전 (ISO 주차 `2026-W31` 기준으로 중복 충전 방지)
- 자정 직후 WorkManager가 어제 세션을 확인 → 미완료이고 프리즈 보유 시 자동 소모, `usedFreeze = true` 기록 후 알림으로 통지

### 알림 스케줄
```
1) DailyReminderWorker   — 매일 reminderHour:reminderMinute
   조건: 오늘 세션 미완료 → "🔥 27일 스트릭이 걸려 있어요. 10문장만!"
   
2) LastCallWorker        — 매일 22:30
   조건: 오늘 세션 미완료 → "오늘 30분 남았습니다"
   
3) StreakGuardWorker     — 매일 00:10
   어제 세션 판정 → 프리즈 소모 또는 스트릭 리셋 처리
```

**구현 주의**
- `PeriodicWorkRequest`는 최소 주기가 15분이며 정확한 시각을 보장하지 않으므로, **`OneTimeWorkRequest`를 매번 다음 실행 시각으로 재예약**하는 방식(self-rescheduling)을 사용하세요.
- `BOOT_COMPLETED` 브로드캐스트 수신 시 재예약 필요.
- Android 13+ 최초 실행 시 `POST_NOTIFICATIONS` 권한 요청.
- 온보딩에 배터리 최적화 예외 요청 화면 포함 (`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`).

---

## 10. 자막 파서

```
지원 포맷: .srt, .vtt

SRT 파싱:
  블록 구분 = 빈 줄
  1행: 인덱스(무시)
  2행: "00:00:12,345 --> 00:00:15,678"  → startMs, endMs
  3행~: 자막 텍스트 (여러 줄이면 공백으로 join)

전처리:
  - <i>, <b>, {\an8} 등 태그 제거
  - 화자 표시 "RACHEL:" 패턴은 별도 필드로 분리 후 본문에서 제거
  - 대괄호 지시문 [laughing], (SIGHS) 제거
  - HTML 엔티티 디코드 (&amp; &quot; 등)
  - 텍스트가 비면 해당 라인 스킵
  - 인접 라인의 시간이 겹치면 뒤쪽 startMs를 앞쪽 endMs로 보정

구간 반복 여유(padding): startMs - 200ms, endMs + 300ms
  (자막 타임코드가 실제 발화보다 타이트한 경우가 많음)
```

---

## 11. 안드로이드 실전 함정 (구현 시 반드시 반영)

| # | 함정 | 대응 |
|---|---|---|
| 1 | **삼성 키보드 자동완성이 오답을 몰래 고침** | 받아쓰기 입력창에 `KeyboardOptions(autoCorrectEnabled = false, keyboardType = KeyboardType.Ascii)`. 뷰 기반이면 `TYPE_TEXT_FLAG_NO_SUGGESTIONS`. **이걸 빼먹으면 앱 전체가 무의미해짐** |
| 2 | 키보드(IME)가 채점 결과를 가림 | `Modifier.imePadding()` + 재생/제출 버튼을 키보드 바로 위 고정 바에 배치 |
| 3 | SAF URI가 재부팅 후 무효화 | `takePersistableUriPermission()` 호출 필수 |
| 4 | ExoPlayer 구간 반복이 부정확 | `seekTo()` 후 `SEEK_PARAMETER_EXACT` 사용, 위치 폴링은 50ms 간격 |
| 5 | 유튜브 임베드의 seek 정밀도가 낮음 | 유튜브 소스는 ±0.5초 오차를 감안, 구간 padding을 로컬보다 넓게 |
| 6 | 화면 회전/앱 전환 시 진행 상태 소실 | 단계 진행 상태를 ViewModel `SavedStateHandle`에 보존 |
| 7 | 다크모드에서 색상 피드백 대비 부족 | 채점 색상은 라이트/다크 각각 별도 팔레트 정의 |

**보너스 UX**
- 재생 속도 토글 (1.0x / 0.75x / 0.5x)
- 하드웨어 볼륨 업 키에 "다시 듣기" 매핑 → 타이핑 중 손 안 옮기고 재생
- 제출은 키보드 Enter(ImeAction.Done)로도 가능

---

## 12. 개발 순서 (마일스톤)

각 단계가 **독립적으로 동작하는 앱**이어야 합니다. 한 단계 끝날 때마다 빌드·실행 확인.

| M | 범위 | 완료 기준 |
|---|---|---|
| **M1** | Room 스키마 + SRT 파서 + 로컬 영상 구간 반복 | 파일 2개 넣으면 대사 목록이 뜨고, 탭하면 그 구간만 반복 재생됨 |
| **M2** | 받아쓰기 채점 엔진 (§6) + 유닛 테스트 | §6.5 테스트 케이스 전부 통과 |
| **M3** | 4단계 학습 루프 UI + 진행 상태 저장 | 문장 10개를 1→4단계로 끝까지 진행 가능 |
| **M4** | 힌트 사다리 + 청크 분할 | 5회 시도 시 정답 노출, 20단어 문장이 자동 분할됨 |
| **M5** | 스트릭 + WorkManager 알림 + 홈 화면 | 21시에 알림 오고, 연속 일수가 정확히 계산됨 |
| **M6** | 유튜브 소스 지원 | 유튜브 영상 + 수동 srt로 학습 가능 |
| **M7** | SRS 복습 큐 + 통계 화면 | 어제 틀린 문장이 오늘 복습에 등장 |
| **M8** | 위젯 + 백업/복원 + 다크모드 정리 | — |

**M1~M3까지가 MVP.** 여기까지만 되어도 매일 쓸 수 있습니다.

---

## 13. 수용 기준 (Definition of Done)

- [ ] 비행기 모드에서 로컬 영상 학습이 완전히 동작한다
- [ ] 받아쓰기 입력 시 키보드 자동완성/예측이 뜨지 않는다
- [ ] 채점 결과에서 누락 단어가 `___`로, 잉여 단어가 취소선으로 구분되어 보인다
- [ ] `I'm` ↔ `I am`이 동일하게 처리된다
- [ ] 앱을 껐다 켜도 진행 중이던 문장의 단계가 유지된다
- [ ] 하루 미학습 시 21:00에 알림이 실제 갤럭시 기기에서 온다 (배터리 최적화 예외 상태)
- [ ] 스트릭 프리즈 사용 후 연속 일수가 끊기지 않는다
- [ ] 자동 생성 자막으로 등록한 소스는 3단계가 비활성화된다
- [ ] 20단어 이상 문장이 자동으로 2조각 이하로 분할된다

---

## 부록 A. Claude Code 첫 프롬프트 예시

```
이 사양서(ShadowDict_프로젝트_사양서.md)를 읽고 M1을 구현해줘.

- Kotlin + Jetpack Compose, Room, Media3
- 범위: Room 엔티티 전체 정의 + SRT/VTT 파서 + 로컬 파일 선택(SAF) 
  + 대사 목록 화면 + 구간 반복 재생
- 채점 엔진, 학습 4단계, 알림은 아직 만들지 마
- 파서는 유닛 테스트를 함께 작성해줘
- 완료 후 빌드 가능한 상태인지 확인하고, 다음 단계에서 
  내가 확인해야 할 것을 알려줘
```

**M2 진입 시:**
```
사양서 §6의 받아쓰기 채점 알고리즘을 구현해줘.
§6.5의 테스트 케이스 7개를 먼저 유닛 테스트로 작성하고,
그것을 통과하는 방향으로 구현해줘. (TDD)
```
